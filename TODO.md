# TODO: Fix Back Button in AlfaTopupController

## Steps to Complete:
- [ ] Edit `src/main/java/com/app/AlfaTopupController.java` to change the `handleBack()` method from `App.showHome()` to `App.setRoot("Topup")`.
- [ ] Verify that the change navigates to the Top-Up main screen and maintains balance synchronization.
